package core.elements.table;

import core.elements.BaseElement;
import org.openqa.selenium.WebElement;

public class Cell extends BaseElement {

    public Cell(WebElement element, String name) {
        super(element, name);
    }
}
