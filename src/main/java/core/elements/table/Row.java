package core.elements.table;

import core.elements.BaseElement;
import org.openqa.selenium.WebElement;

public class Row extends BaseElement {

    public Row(WebElement element, String name) {
        super(element, name);
    }
}
